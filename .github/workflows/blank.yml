# This is a basic workflow to help you get started with Actions

name: publish plugin

# Controls when the workflow will run
on:
  # Triggers the workflow on push or pull request events but only for the "main" branch
  release:
    types: [ published ]
  push:
    tags:
      - 'v*'  # 仅在标签匹配 'v*' 模式时触发

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v4

      - name: 🐼 Set up JDK 18
        uses: actions/setup-java@v3
        with:
          java-version: '18'
          distribution: 'temurin'


      - name: 🔪 Create key=value to gradle.properties
        run: |
          echo 🔪 Create key=value to gradle.properties
          java --version
          echo "gradle.publish.key=${{ secrets.GRADLE_KEY }}" >> gradle.properties
          echo "gradle.publish.secret=${{ secrets.GRADLE_SECRET }}" >> gradle.properties
          echo "mavenCentralUsername=${{ secrets.MAVEN_NAME }}" >> gradle.properties
          echo "mavenCentralPassword=${{ secrets.MAVEN_PASSWORD }}" >> gradle.properties
          echo "📸 Reading gradle.properties..."
          cat gradle.properties

      # Runs a single command using the runners shell
      - name: 🔥 publishing plugin
        run: |
          chmod +x gradlew
          ./gradlew :conventions:publishPlugins

#      - name: Create key=value to local.properties
#        run: |
#          echo "apiKey=${{ secrets.API_KEY }}" > local.properties
#
#      - name: Read local.properties
#        id: read_local_properties
#        run: |
#          apiKey=$(grep 'apiKey' local.properties | cut -d'=' -f2)
#          echo "API_KEY=$apiKey" >> $GITHUB_ENV
